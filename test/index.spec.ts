// TODO: write tests
describe('index', () => {
  describe('connect', () => {
    it('should return an instance of the Connection class', () => {
      const message = 'Hello';

      expect('Hello world').toMatch(message);
    });
  });
});
